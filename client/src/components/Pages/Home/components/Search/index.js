import IconButton from '@material-ui/core/IconButton/IconButton';
import React from 'react';

const Search = () => (
  <IconButton>
    <Search color={'primary'} fontSize={'large'} titleAccess={'Search'}/>
  </IconButton>
);
export default Search;
